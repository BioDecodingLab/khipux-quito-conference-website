---
// Cookie Notice Banner Component - Informational (Implied Consent)
---

<div
  id="cookie-banner"
  class="fixed bottom-4 left-4 right-4 z-50 hidden max-w-sm mx-auto md:left-auto md:right-6 md:bottom-6 md:mx-0"
>
  <div class="bg-khipu-gray border-khipu-orange border-2 p-6 shadow-2xl">
    <div class="flex flex-col gap-4">
      <div class="text-khipu-white text-sm leading-relaxed">
        <span class="mb-2 block text-lg">🍪</span>
        We use cookies to analyze site traffic and improve your experience. By continuing to use this site, you agree to our use of cookies.
      </div>
      <button
        id="cookie-ok"
        class="bg-khipu-orange hover:bg-khipu-orange/90 w-full py-3 text-sm font-bold text-white uppercase tracking-wide transition"
      >
        OK, Got it!
      </button>
    </div>
  </div>
</div>

<script>
  // Cookie Notice Logic - Simple dismiss
  (function() {
    const banner = document.getElementById('cookie-banner');
    const okBtn = document.getElementById('cookie-ok');
    
    // Check if user already dismissed the banner
    const dismissed = localStorage.getItem('cookie-notice-dismissed');
    
    if (!dismissed) {
      // Show banner if not dismissed yet
      banner?.classList.remove('hidden');
      banner?.classList.add('flex');
    }
    
    // OK button - just dismiss the banner
    okBtn?.addEventListener('click', function() {
      localStorage.setItem('cookie-notice-dismissed', 'true');
      banner?.classList.add('hidden');
    });
  })();
</script>
